/* ============================================================
   init.js — تهيئة الصفحة عند التحميل
   ============================================================ */
'use strict';

document.addEventListener('DOMContentLoaded', () => {
  initUI();   // scroll reveal, FAQ, tabs, calculator, sticky
  initForm(); // checkbox cards, form steps
});
